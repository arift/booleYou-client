angular.module('booleYou', ['ionic'])
.controller('PopupCtrl',function($scope, $ionicPopup, $timeout) {

// Triggered on a button click, or some other target
$scope.showPopup = function() {
    console.log("here");

  $scope.data = {}

  // An elaborate, custom popup
  
 };
